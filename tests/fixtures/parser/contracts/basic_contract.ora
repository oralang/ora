contract BasicContract {
    storage {
        owner: address,
        balance: u256,
    }
    
    pub fn get_owner() -> address {
        return owner;
    }
    
    pub fn get_balance() -> u256 {
        return balance;
    }
    
    fn internal_function() {
        // Internal logic
    }
}