contract SwitchExprNested {
    pub fn demo() {
        let x: u256 = 2;
        let y: u256 = switch (x) {
            0 => 1,
            1 => switch (x + 1) { 2 => 20, else => 21, },
            2...5 => switch (x) { 2 => 100, 3 => 101, else => 102, },
            else => 9,
        };
        _ = y;
    }
}
