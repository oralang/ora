contract BadSwitchExprBlockArm {
    pub fn demo() {
        let x: u256 = 1;
        let y: u256 = switch (x) {
            1 => { 10 }, // block arm not allowed in switch expression
            else => 0,
        };
        _ = y;
    }
}
