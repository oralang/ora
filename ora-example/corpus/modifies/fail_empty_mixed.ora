contract Vault {
    storage total: u256 = 0;

    pub fn bad(value: u256)
        modifies()
        modifies total
    {
        total = value;
    }

    pub fn also_bad(value: u256)
        modifies total
        modifies()
    {
        total = value;
    }
}
