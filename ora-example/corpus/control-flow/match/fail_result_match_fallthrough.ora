error Failure(code: u256);

pub contract ResultMatchFallthrough {
    pub fn inspect(value: Result<u256, Failure>) -> u256 {
        match (value) {
            Ok(inner) => {
                return inner;
            },
            Err(_) => {
                let ignored: u256 = 0;
            }
        }
    }
}
