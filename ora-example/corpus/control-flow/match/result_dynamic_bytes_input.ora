error Failure(code: u256);

contract MatchDynamicBytesInput {
    pub fn consume(value: Result<bytes, Failure>) -> u256 {
        return match (value) {
            Ok(inner) => 1,
            Err(err) => err.code,
        };
    }
}
