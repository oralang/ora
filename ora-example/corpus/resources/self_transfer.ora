resource TokenUnit = u256;
comptime const std = @import("std");

contract SelfTransfer {
    storage var balances: map<address, Resource<TokenUnit>>;

    pub fn transfer_self(account: address, amount: TokenUnit)
        requires balances[account] >= amount
    {
        @move(balances[account], balances[account], amount);
    }
}
