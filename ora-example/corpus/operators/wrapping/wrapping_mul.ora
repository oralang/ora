contract WrappingMul {
    pub fn wrapping_mul_u8(a: u8, b: u8) -> u8 {
        let result: u8 = a *% b;
        return result;
    }

    pub fn wrapping_mul_u256(a: u256, b: u256) -> u256 {
        let result: u256 = a *% b;
        return result;
    }
}
