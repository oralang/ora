contract NoReturnTest {
    storage var counter: u256;
    
    pub fn increment() {
        counter = counter + 1;
    }
    
    pub fn add(a: u256, b: u256) {
        counter = a + b;
    }

    pub fn getCounter() -> u256 {
        return counter;
    }
}