// Test file for enum expressions without parentheses
enum TestValues {
    // Simple literal values
    Value1 = 5,
    
    // Non-parenthesized expression
    Value2 = 5 + 15,
    
    // Multiple operations without parentheses
    Value3 = 10 * 2 / 2,
    
    // Mixed operations with different precedence
    Value4 = 5 + 3 * 2,
    
    // Parenthesized expressions (for comparison)
    Value5 = (7 + 3) * 2,
    
    // More complex expression
    Value6 = 100 - 50 + 25 * 2
}

// Function to use the enum values
fn test_enum_values() {
    const v1 = TestValues.Value1;
    const v2 = TestValues.Value2;
    const v3 = TestValues.Value3;
    const v4 = TestValues.Value4;
    const v5 = TestValues.Value5;
    const v6 = TestValues.Value6;
    
    // Expected results:
    // v1 = 5
    // v2 = 20
    // v3 = 10
    // v4 = 11
    // v5 = 20
    // v6 = 100
}
