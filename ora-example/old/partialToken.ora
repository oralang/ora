contract MyContract {

    enum Status {
        Active,
        Inactive,
        Pending,
    }

    pub fn init() {
    }

    pub fn status(x: u256) u256 {
        return x + 1;
    }

    pub fn foo() void {
        // Function body
    }
}