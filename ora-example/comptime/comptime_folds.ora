contract ComptimeFolds {

    pub fn tuple_access() -> u256 {
        const folded: u256 = 30;
        return folded;
    }

    pub fn tuple_destructure() -> u256 {
        const folded: u256 = 7;
        return folded;
    }
}
