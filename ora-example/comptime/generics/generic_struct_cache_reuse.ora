contract GenericStructCacheReuse {
    struct Pair(comptime T: type) {
        first: T;
        second: T;
    }

    pub fn make_pair_one(a: u256, b: u256) -> Pair(u256) {
        return Pair(u256) { first: a, second: b };
    }

    pub fn make_pair_two(a: u256, b: u256) -> Pair(u256) {
        let p: Pair(u256) = Pair(u256) { first: a, second: b };
        return p;
    }

    pub fn make_pair_u8(a: u8, b: u8) -> Pair(u8) {
        return Pair(u8) { first: a, second: b };
    }
}
