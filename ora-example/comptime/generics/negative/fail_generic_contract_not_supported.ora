contract Vault(comptime TokenType: type) {
    balances: map(address, TokenType);

    pub fn deposit(amount: TokenType) {
        balances[std.msg.sender()] = balances[std.msg.sender()] + amount;
    }
}
