contract FailGenericPlusStringSameType {
    fn add(comptime T: type, a: T, b: T) -> T {
        // INVALID: instantiated as address + address.
        return a + b;
    }

    pub fn bad(a: address, b: address) -> address {
        return add(address, a, b);
    }
}
