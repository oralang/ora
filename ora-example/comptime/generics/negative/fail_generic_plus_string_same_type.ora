contract FailGenericPlusStringSameType {
    fn add(comptime T: type, a: T, b: T) -> T {
        // INVALID: instantiated as string + string.
        return a + b;
    }

    pub fn bad() -> string {
        return add(string, "ora", "lang");
    }
}
